# Units

## Verified Discoveries
- No dedicated `unit` help tag exists
- `tags=units` returns no commands
- Units appear to be managed through broader tags like `jobs` and `professions`

## Test
```bash
bonsai-df-probe --timeout 30 -- dfhack-run help soldier
```
