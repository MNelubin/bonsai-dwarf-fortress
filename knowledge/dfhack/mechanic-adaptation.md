### Mechanism: Unit Cave Adaptation

VERIFIED `dfhack-run adaptation level 3 12 14`

`adaptation` command adjusts a unit's cave adaptation level.

- `level` parameter specifies target adaptation level (0-3)
- Two positional coordinates identify the unit
- Command succeeds silently when applied

Output confirms deterministic behavior with immediate level change.
