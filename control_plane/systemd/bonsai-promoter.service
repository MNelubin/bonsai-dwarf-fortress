[Unit]
Description=Bonsai trusted candidate gate and GitHub promoter
After=network-online.target tailscaled.service bonsai-api.service
Wants=network-online.target tailscaled.service

[Service]
Type=simple
User=bonsai
Group=bonsai
WorkingDirectory=/srv/bonsai-control/repo
EnvironmentFile=/etc/bonsai-control/app.env
ExecStart=/opt/bonsai-control/current/venv/bin/bonsai-promoter
Restart=always
RestartSec=5
NoNewPrivileges=true
PrivateTmp=true
ProtectSystem=strict
ProtectHome=read-only
ReadWritePaths=/srv/bonsai-control /srv/bonsai-control/logs

# systemd-journald does not run in these unprivileged containers - it exits
# 243/CREDENTIALS - so StandardOutput=journal discarded everything these three
# services ever said. The 38-day outage and the promoter's ten-second retry
# storm both had to be reconstructed from the database event ledger because of
# it. Log to a file the container can actually write.
StandardOutput=append:/srv/bonsai-control/logs/bonsai-promoter.log
StandardError=append:/srv/bonsai-control/logs/bonsai-promoter.log

[Install]
WantedBy=multi-user.target
